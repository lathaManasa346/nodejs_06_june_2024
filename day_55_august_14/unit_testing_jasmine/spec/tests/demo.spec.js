const { isPrime, fibonacci, isEven, cars } = require("../../util")

describe("This is a test suite for demo file", () => {
    it("This is a test case from demo file", () => {
        console.log("it from demo file")
    })
})